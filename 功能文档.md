# 凯中发布工具功能文档

## 1. 文档说明

本文档依据当前代码整理已经实现的功能、业务规则、接口和数据边界。系统由WPF客户端、无界面Windows服务端和独立更新器组成。

## 2. 系统架构

```text
WPF客户端
  ├─ servers.db：服务器配置
  ├─ log.db：发布/回滚日志
  ├─ GetDLL.txt：指定DLL清单
  └─ SMOMDLL：本地待发布文件
          │ HTTP :5050
          ▼
Windows服务端 KaiZhongReleaseToolServer
  ├─ 健康检查、目录浏览
  ├─ 上传、备份、发布、回滚
  ├─ Windows服务启停
  └─ 服务端自动升级
          │
          ▼
独立Updater：备份旧程序、覆盖新版、重启、健康检查、失败恢复
```

## 3. 客户端基础功能

### 3.1 单实例和系统托盘

- 命名互斥量防止重复运行；
- 重复启动显示提示；
- 主窗口不能直接关闭；
- 通过系统托盘右键退出；
- 双击主界面提示文字打开程序目录；
- 使用凯中程序图标和版权信息。

### 3.2 服务器列表

- 显示梯队、服务器、状态、服务器IP；
- 梯队显示数字1或2；
- 在线整行绿色、离线整行红色、未检测/检测中整行黄色；
- 鼠标悬停字体蓝色加粗；
- 双击服务器进入编辑；
- 左侧栏支持拖动宽度；
- 横向和纵向滚动条使用自定义样式。

### 3.3 服务器右键菜单

- 远程服务器；
- 新增服务器；
- 编辑服务器；
- 删除服务器；
- 刷新状态；
- 分组管理；
- 发布回滚；
- 升级服务端。

删除操作必须输入“我确认删除”。菜单带图标及双分割线。

### 3.4 分组管理和过滤

- 新增、重命名、删除独立分组；
- 重命名时同步修改组内服务器；
- 被服务器使用的分组不能删除；
- 服务器配置只能选择已有分组；
- 主列表按“全部”或多个分组复选框过滤。

### 3.5 在线状态

- 首次加载自动检测全部服务器；
- 支持手工刷新；
- 通过HTTP健康检查判断状态；
- 保留检测状态详情。

## 4. 服务器配置功能

顶部显示：

```text
当前编辑>> 服务器名称 服务器IP
```

右上角无文字滑动开关控制编辑：

- 关闭为灰色，只读但可切换页签；
- 开启为深蓝色，可编辑和保存；
- 每次打开默认关闭；
- 保存后恢复关闭。

### 4.1 基本信息

- 服务器名称；
- 分组；
- IP或主机名；
- 服务端口；
- 远程桌面账户、密码、端口；
- 发布梯队1/2，默认2且不允许空。

### 4.2 发布前备份配置

四个应用各有路径和服务：

- `SIE.ScheduleServer`；
- `SIE.WebApiHost`；
- `WebClient`；
- `WpfClient`。

功能包括：

- 手工填写服务端路径；
- 通过服务端目录浏览器选择磁盘和文件夹；
- 多个服务名支持中英文逗号；
- 独立配置备份ZIP目标目录；
- 路径为空代表该服务器跳过该应用；
- 非WpfClient应用配置路径后必须有有效服务；
- WpfClient允许不配置服务。

### 4.3 发布梯队

- 同组双备服务器支持分为第1、第2梯队；
- 第1梯队先发布；
- 第1梯队是第2梯队的质量闸门；
- 旧数据库自动新增梯队字段并默认设为2。

## 5. SQLite数据功能

### 5.1 `servers.db`

位于客户端EXE同级，保存服务器、分组、梯队、连接、远程桌面、四个发布路径、四组服务名和备份目录。支持自动建表、旧表补列，以及从旧`%LOCALAPPDATA%`位置迁移。

数据库可复制给其他用户直接复用配置。远程桌面密码按当前需求明文保存。

### 5.2 `log.db`

- 与服务器配置库完全分离；
- 使用WAL模式；
- 保存日志集和日志明细；
- 记录时间、服务器、级别和消息；
- 服务端不保存业务日志数据库。

## 6. 批量服务器选择

上传发布、批量回滚和服务端升级支持：

- 单选、多选、全选；
- 按动态分组选择；
- 全部显示；
- 仅显示勾选；
- 仅显示未勾选；
- 显示选择数量；
- 在线绿色、离线红色；
- 过滤显示不改变勾选状态。

## 7. 远程桌面功能

- 使用系统`mstsc`；
- 删除与当前IP/端口匹配的旧通用凭据和Windows凭据；
- 写入当前远程桌面凭据；
- 支持本地账户和AD域账户；
- `/`自动转为`\`；
- `./administrator`转为`服务器IP\administrator`；
- 3389不附加端口，其他端口附加；
- 不包含内嵌RDP ActiveX。

## 8. SMOM路径识别

- 输入或选择文件夹；
- 文本变化自动识别；
- 可从深层目录向上寻找项目根；
- 自动定位四个项目Debug输出目录；
- 识别测试结构`SMOM.KAIZHONG\SMOM.KAIZHONG`；
- 识别正式结构`SMOM.KAIZHONG-Prod\SMOM.KAIZHONG`；
- 测试/正式目录颜色强调和文字标识；
- 识别失败显示“未能识别路径”。

## 9. DLL获取

### 9.1 指定DLL

- 读取EXE同级`GetDLL.txt`；
- 不存在时自动创建；
- 空文件阻断并红色提示；
- 每行一个DLL名；
- 自动补`.dll`；
- 大小写不敏感；
- 自动去重；
- 分别在四个项目输出目录查找。

### 9.2 全量DLL

- 获取所有以`SIE`开头的DLL；
- 分应用统计预计数量和实际数量。

### 9.3 目录清理

- 自动创建四个`SMOMDLL`应用目录；
- 每次获取前清空；
- 失败重试5次、间隔1秒；
- 任一目录清空失败阻断复制；
- 标题蓝色加粗、数量橙色加粗、失败红色；
- 结果不写日志数据库。

## 10. 选择性上传

客户端按每台服务器的路径配置创建上传ZIP：

- 路径已填写：上传对应应用目录；
- 路径未填写：不上传该应用；
- 所有路径为空：跳过网络上传；
- 相同应用组合复用同一个临时ZIP；
- 保留文件和子目录结构；
- 显示实际上传文件数；
- 服务器并发上传，谁完成谁先显示；
- 失败重试5次、间隔3秒；
- 任一服务器失败阻断下一步。

服务端收到包后清空公共`SMOMDLL`并解压本次内容。

## 11. 配置检查

每个应用检查文件、备份路径、服务：

| 符号 | 文件 | 备份路径 | 服务 |
|---|---|---|---|
| ✔ | 有文件 | 已填写且真实存在 | 已填写且所有服务存在 |
| ○ | 无文件 | 未填写 | 未填写 |
| × | 不使用 | 已填写但不存在/无效 | 至少一个服务不存在 |

阻断条件：

1. 配置路径但目录不存在；
2. 配置服务但至少一个服务不存在；
3. 非WpfClient应用配置路径却没有服务；
4. 服务端请求失败或结果无效。

不阻断：

- 有文件但未配置路径，该服务器跳过该应用；
- WpfClient配置路径但服务为空。

所有选中服务器必须全部通过。失败原因在阶段末尾集中红色显示，多原因和多服务器分行。

## 12. 发布前备份

- 客户端统一生成`yyyyMMdd-HHmmss`时间戳；
- 只备份有本次文件且配置有效路径的应用；
- 校验源路径存在；
- 防止备份目标位于源目录内部；
- ZIP按应用建立根目录并保留子目录；
- 返回备份文件名和数量；
- 任一服务器失败阻断发布。

## 13. 分梯队发布

完整流程：

1. 上传文件；
2. 检查服务器配置；
3. 备份文件；
4. 发布应用程序。

第4步按梯队执行：

```text
第1梯队：并发停止服务 → 并发覆盖 → 并发启动服务
第2梯队：并发停止服务 → 并发覆盖 → 并发启动服务
```

规则：

- 同一梯队服务器并发；
- 停服失败不覆盖，并恢复已停止服务；
- 文件覆盖无论成功与否都启服；
- 第1梯队任何服务器的停服、覆盖、启服或请求失败都阻断第2梯队；
- 最后一行红色粗体提示阻断；
- 第1梯队全部成功才进入第2梯队；
- 启服失败单独醒目提示具体服务。

重试：停服5次；覆盖10次、间隔3秒；启服10次、间隔3秒。

普通应用递归覆盖目录。WpfClient更新目标`Plugins.zip`，定位`Manifest.xml`的Plugins模块并把版本号最后一段加1。

## 14. 发布回滚

- 支持多服务器；
- 每台服务器分别读取备份ZIP并选择版本；
- 离线或未选择版本会阻断整个批次；
- 回滚前不创建新备份；
- 停止对应服务；
- 还原ZIP中的应用目录；
- 无论还原成功失败都启动服务；
- 文件还原失败重试10次；
- 启服失败重试10次；
- WpfClient返回当前版本；
- 启服失败在完成后红色提醒。

## 15. 日志功能

- 发布日志名：`PushyyyyMMdd-HHmmss`；
- 回滚日志名：`RollBackyyyyMMdd-HHmmss`；
- 步骤标题记录实际时间；
- 各服务器异步结果实时显示；
- 发布和回滚写入`log.db`；
- 日志查看窗口按创建时间选择日志集；
- 回滚日志逐台记录备份集；
- 本次发布DLL清单只写数据库、当前结果区隐藏；
- DLL获取不写数据库。

## 16. Windows服务端

### 16.1 运行方式

- 无界面Windows服务；
- 开机自动启动；
- 默认监听所有网卡5050；
- Kestrel和multipart不设固定上传上限；
- 上传临时目录固定到ProgramData，避免域用户临时会话目录问题；
- BAT提供安装、启动、重启、停止、卸载；
- 更新器不是服务。

### 16.2 公共目录

```text
%ProgramData%\KaiZhong\KaiZhongReleaseTool\SMOMDLL
%ProgramData%\KaiZhong\KaiZhongReleaseTool\Temp
%ProgramData%\KaiZhong\KaiZhongReleaseTool\Update
```

### 16.3 远程目录浏览

- 枚举服务器磁盘；
- 枚举子目录；
- 返回当前路径和上级路径；
- 用于服务器配置页选择目录。

## 17. 服务端通用指令能力

服务端保留统一`/api/command`接口。当前客户端主界面的任务参数区已隐藏，以下主要属于底层接口能力：

### 文件功能

- 移动；
- 复制；
- 覆盖粘贴；
- UTF-8修改；
- 删除文件或递归删除文件夹；
- 单文件时间戳备份；
- 创建文件夹；
- 只压缩第一层文件；
- 压缩完整文件和子文件夹；
- ZIP覆盖解压；
- UTF-8读取文本，最大10MB；
- 接收完整文件夹上传。

### 服务功能

- 获取全部服务；
- 获取指定服务状态；
- 停止；
- 启动；
- 重启。

服务操作通过无窗口`sc.exe`执行，返回标准输出、错误输出和退出码。服务名字符受限制以降低命令拼接风险。

## 18. 服务端自动升级

- 客户端选择ZIP和多台服务器；
- 计算SHA-256；
- 服务端保存并校验升级包；
- 启动独立Updater并停止自身；
- Updater等待旧进程退出；
- 解压暂存、备份安装目录、覆盖新版；
- 启动服务并最多等待90秒健康检查；
- 失败时恢复旧目录并尝试启动旧服务；
- 打包脚本在发布失败时终止，避免生成不完整包。

## 19. HTTP接口

| 方法 | 路径 | 功能 |
|---|---|---|
| GET | `/` | 健康检查、API版本、程序版本 |
| GET | `/api/directories` | 浏览磁盘和目录 |
| POST | `/api/command` | 通用文件/服务指令 |
| POST | `/api/upload-folder` | 接收文件夹ZIP |
| POST | `/api/deploy/smomdll` | 接收待发布文件 |
| POST | `/api/deploy/check` | 检查文件、路径、服务 |
| POST | `/api/deploy/backup` | 创建发布前备份 |
| POST | `/api/deploy/stop` | 停止发布服务 |
| POST | `/api/deploy/publish` | 覆盖应用文件 |
| POST | `/api/deploy/start` | 启动发布服务 |
| POST | `/api/deploy/backups` | 列出备份ZIP |
| POST | `/api/deploy/rollback-stop` | 停止回滚服务 |
| POST | `/api/deploy/rollback-files` | 还原备份文件 |
| POST | `/api/deploy/rollback-start` | 启动回滚服务 |
| POST | `/api/system/update` | 服务端自动升级 |

代码还保留组合式`/api/deploy/apply`和`/api/deploy/rollback`。当前批量客户端主要使用分阶段接口，以便实时显示每步结果。

## 20. 安全和权限边界

- HTTP及升级接口无认证；
- 网络传输未使用HTTPS；
- 远程桌面密码明文保存在客户端SQLite；
- 服务端能够覆盖、递归删除文件并控制Windows服务；
- 必须用防火墙限制5050来源IP；
- 必须限制客户端目录和数据库访问；
- Windows服务账户只授予所需目录和服务权限；
- 不应把服务端放在临时目录；
- 不应在服务运行时手工覆盖DLL；
- 正式发布前必须核对服务器、梯队、路径、服务名、备份目录和回滚版本。

## 21. 主要代码文件

| 文件 | 职责 |
|---|---|
| `MainWindow.xaml(.cs)` | 主界面、DLL获取、发布、回滚、升级、日志 |
| `ServerEditWindow.xaml(.cs)` | 服务器、路径、服务、梯队配置 |
| `ServerRepository.cs` | `servers.db`建表、迁移、CRUD |
| `LogRepository.cs` | `log.db`日志集和明细 |
| `ServerHost.cs` | 服务端HTTP和发布/回滚业务 |
| `CommandExecutor.cs` | 通用文件及服务指令 |
| `AppPaths.cs` | ProgramData目录 |
| `ReleaseServerWorker.cs` | Windows服务生命周期 |
| `KaiZhongReleaseTool.Server.Updater/Program.cs` | 自动升级和失败恢复 |
| `Scripts/Build-ServerPackage.ps1` | 生成首次安装目录和升级ZIP |
| `Scripts/01-05*.bat` | 安装和管理Windows服务 |
